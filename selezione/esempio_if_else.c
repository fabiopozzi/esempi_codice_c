#include <stdio.h>

int main() {

    int n = 10;
    // se il numero e' positivo stampo "POSITIVO"
    if (n > 0) {
        printf("POSITIVO");
    }
    printf("ARRIVEDERCI!");

    // cosa succede se n fosse == -20?
    return 0;
}