#include <stdio.h>

int main() {

    int n = 10;
    // se il numero e' positivo stampo "POSITIVO"
    if (n > 0) {
        printf("POSITIVO\n");
    } else {
        printf("NEGATIVO\n");
    }
    printf("ARRIVEDERCI!");

    // cosa succede se n fosse == -20?
    return 0;
}
