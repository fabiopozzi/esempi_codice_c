#include <stdio.h>

int main()
{
    /* commento
     su
    piu'
    linee
    */
    int somma = 0; // DICHIARAZIONE + INIZIALIZZAZIONE
    int g; // DICHIARAZIONE

    printf("Somma: %d", somma);
    g = 10;
    somma = somma + g;

    // leggo n
    // leggo n numeri
    // sommo i numeri maggiori di 50
    // stampo il risultato


    return 0;
}
